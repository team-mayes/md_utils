[main]
data_tpl_file = tests/test_data/evbd2d/serca_deprot_edited.data
dump_list_file = tests/test_data/evbd2d/serca_dump_list_incomp.txt
output_directory = tests/test_data/evbd2d/
water_o_type = 2
water_h_type = 1
h3o_o_type = 5
h3o_h_type = 6
prot_res_mol_id = 908
# When the protonatable residue is protonated, we need to obtain the atom number of that proton
# To do that, note the atom type. If any other atoms in that residue have that same atom type,
# *list* them, so they will be ignored
prot_h_type = 7
# prot_ignore_h_atom_nums is if there are other atoms in the prot_res_mol_id with type prot_h_type
#   This is the actual index (base 1)
#   If there are multiple atoms, separate with commas
prot_ignore_h_atom_nums = 13997
# For debugging total charge, list the atom ids of:
last_p1 = 15436
last_p2 = 16327
lone_ion = 16328
last_lipid = 65640
last_hyd = 65644
last_water = 213877
last_ion1  = 213992
# atoms numbers whose type and/or charge can change upon protonation
prot_res_type_ignore_atoms = 14003,14006,14007,14008