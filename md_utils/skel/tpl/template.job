#!/bin/bash
#SBATCH --job-name={job_name}
#SBATCH -t {walltime}:00:00
#SBATCH -p {partition}
#SBATCH -N {nnodes} --tasks-per-node={nprocs}
#SBATCH --mail-type=END,FAIL
set echo
set -x

cd $SLURM_SUBMIT_DIR
## Your commands here:
